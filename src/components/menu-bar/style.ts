import styled from '@emotion/styled'

export const MenuWrapper = styled.div`
	.logo {
		display: block;
		height: 50px;
		background: url(${require('@/assets/img/logo.png')}) center center no-repeat;
		margin-top: 10px;
	}
`
